[pytest]
# 服务层独立测试配置。
#
# 刻意不继承仓库根 pytest.ini：那份配置的 testpaths 只覆盖上游，
# 且带 --cov-fail-under=90，会干扰本层测试。
testpaths = service/tests
asyncio_mode = auto
addopts = -q -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
