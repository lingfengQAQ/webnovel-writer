# Implement：书级知识校准与策展

> 当前状态：in_progress（2026-07-18，作者已批准）。以下步骤按批次执行；本任务不在内容批次中引入旧格式兼容。

## 门 0：启动前检查

- [x] 0.1 PRD 已完成 convergence，19 个题材、24 个基础流派、`青春 / 竞技 / 势力建设` 的范围与设计一致。
- [x] 0.2 `design.md` 的最小字段、正文小节、别名切换、衍生校验、数值配额规则和复合题材协议得到作者审核。
- [x] 0.3 作者明确批准进入实现后，运行 `task.py start 07-10-book-level-knowledge-migration`。
- [x] 0.4 进入 Phase 2 前使用 `trellis-before-dev` 读取后端 spec；inline 模式由主会话实现和检查，不填充 seed-only jsonl。

## 批次 1：调用者与校验器窄改动

- [x] 1.1 先补失败测试：流派兼容范围为空时不提醒；`类型=衍生` 无实际世界副题材时拒绝；数值节奏配额缺统计对象/目的/失效条件时拒绝。
- [x] 1.2 修改 `v7/src/knowledge/index.js`，把流派空兼容范围解释为无需窄范围提醒；不改变名称归一、候选上限或排序。
- [x] 1.3 修改 `v7/src/knowledge/contract.js`，加入衍生副题材和数值配额两项条件校验；不增加 front matter 或 payload 字段。
- [x] 1.4 修改 `knowledge-pack` 与建书技能：副题材存在时输出中央融合协议清单；歧义输入继续对谈，不自动猜测。
- [x] 1.5 运行知识索引、命令、契约和 pipeline 定向测试。该批形成独立回滚点。

## 批次 2：题材、流派与路由原子切换

### 2A 格式契约

- [x] 2.1 更新 `test/references/format.test.js`：题材/流派 front matter 只允许 `名称`；按维度要求四个正文小节；每个正式文件与路由一一对应。
- [x] 2.2 增加禁止项：题材/流派不含 `毒点 / 适用题材` 字段，不含通用固定比例或“每 N 章”默认，不含已裁决的歧义别名。

### 2B 题材

- [x] 2.3 建立 `research/题材流派逐条裁决.md`，先记录 16 个现有题材和 5 个新增题材的动作与边界。
- [x] 2.4 按 `核心承诺 / 选择边界 / 本书化问题 / 失败模式` 重写 14 个保留题材。
- [x] 2.5 新增 `武侠 / 现实 / 军事 / 体育 / 灵异`，压力检查设计表列出的相邻题材。
- [x] 2.6 删除题材文件 `快穿.md / 种田.md`；收窄悬疑、科幻、历史、游戏、年代等重叠内容，清除恩怨默认、圣母预设和具体金手指模板。

### 2C 流派

- [x] 2.7 按 `推进引擎 / 组合边界 / 本书化问题 / 失败模式` 重写现有 22 个流派，删除固定章数、阶段、配比、世界规则复制和具体人物组合。
- [x] 2.8 新增 `快穿流.md / 种田经营流.md`；分别验证与穿越流/无限流、年代及各世界题材的边界。
- [x] 2.9 洪荒流、克苏鲁、诡秘流只保留长期引擎，把可独立调用的世界规则候选登记给故事对象任务。

### 2D 路由

- [x] 2.10 一次性更新 `路由.csv`：19 个题材、24 个基础流派、精确别名、必要兼容范围和新文件路径；删除 `全部` 与设计列出的歧义别名。
- [x] 2.11 增加行为测试：快穿/种田不再命中题材但能命中流派；灵异独立命中；惊悚/恐怖/青春/校园/普通竞技保持未命中；复合输入不静默丢维。
- [x] 2.12 运行格式、索引、命令和 pipeline 定向测试；完成题材与流派两份独立内容审查后，才形成 taxonomy 原子回滚点。

## 批次 3：v6 创意约束策展

- [x] 3.1 建立 `research/v6创意资产裁决.md`，覆盖四份 anti-trope 与五份 creativity 文档；每段用标题或旧 ID 唯一定位。
- [x] 3.2 跨九份来源聚类、查重，逐候选执行独有问题、独有动作、独有失败模式、明确触发、可区分五项准入。
- [x] 3.3 删除评分、市场时效、固定创意包、具体案例和普通卖点；把人物、设定、开篇与表现方法候选登记给对应子任务。
- [x] 3.4 仅对通过准入的候选建立 `创意约束/` 条目，front matter 为 `名称 / 一句话`，正文为 `约束机制 / 适用边界 / 本书化问题 / 失败模式`；不设条目数量目标。
- [x] 3.5 为 `一句话` 使用可展示且可被真实问题触发的短语片段，补查询命中、最多三条、拒绝和自定义降级测试。
- [x] 3.6 完成创意约束独立内容审查并运行格式、查询与 pipeline 定向测试。该批可整体撤销为空目录降级。

## 批次 4：复合题材与反同质化验收

- [x] 4.1 用至少两组相同主题材/副题材/流派输入，分别采用不同作者意图或创意约束，形成两份机制不同的作品契约测试样本。
- [x] 4.2 样本逐项核对共同冲突、主题材主线因果、副题材介入条件与独立贡献、规则兼容/隔离、冲突解决和失焦风险。
- [x] 4.3 证明路由输出不能唯一决定创意约束、融合协议、差异化点或关系结算；候选可组合、修改、拒绝和自定义。
- [x] 4.4 验证所有数值节奏都是本书专属，且带统计对象、目的和失效条件；通用条目不存在固定比例与频率。

## 批次 5：文档、分发与全量检查

- [x] 5.1 更新 `v7/references/README.md` 的最终基线数量、字段和调用说明；只在本任务实际改变既有治理契约时才修改 `v7/docs/knowledge/`。
- [x] 5.2 建立 `research/content-review/题材.md`、`流派.md`、`创意约束.md`，关闭所有内容发现或回到对应批次修正。
- [x] 5.3 核对 installer vendoring、治理链接、技能提示、路由、正式目录和测试 fixture 无漂移；章级存量旧分类引用已明确转交篇章知识任务。
- [x] 5.4 执行全量质量检查：569 项全量测试、installer e2e、任务上下文校验和 `git diff --check` 全绿；删除遗留“全部”兼容分支，不以兼容逻辑绕过失败。

## 验证命令

```powershell
node --test v7/test/references/format.test.js
node --test v7/test/knowledge/index.test.js
node --test v7/test/knowledge/query-command.test.js
node --test v7/test/knowledge/contract.test.js
node --test v7/test/knowledge/pipeline.test.js
npm --prefix v7 test
npm --prefix v7 run e2e:install
git diff --check
```

若 `package.json` 的实际脚本名或工作目录与上列不同，以 `v7/package.json` 为真源修正命令，不跳过同等验证。

## 风险与回滚点

| 风险 | 受影响文件 | 回滚边界 |
|---|---|---|
| 条件校验误伤合法契约 | `src/knowledge/contract.js` 及测试 | 撤销批次 1，不回退作品契约结构 |
| 空兼容范围被误判 | `src/knowledge/index.js`、`路由.csv` | 撤销批次 1 或修正对应路由，不恢复 `全部` 占位 |
| 快穿/种田出现双路径或断路 | `题材/`、`流派/`、`路由.csv` | taxonomy 三者整体回滚，不做双读 |
| 题材/流派正文边界漂移 | 43 个书级条目与裁决记录 | 按 taxonomy 原子批次回滚，题材/流派审查记录保留证据 |
| 创意约束形成新模板 | `创意约束/` 与 v6 裁决 | 整批撤销正式条目，保留裁决记录重新策展 |
| installer 或技能漂移 | vendor、skill、README、pipeline 测试 | 与对应内容批次同回滚，禁止源码与分发物分离 |

## 完成条件

- [ ] PRD A1-A10 全部有证据映射。
- [x] 19 个题材、24 个基础流派和所有正式创意约束均有裁决与独立审查；没有双路径、歧义别名或无消费者字段。
- [x] 衍生、复合题材、快穿、种田、悬疑/灵异及数值配额行为测试通过。
- [x] 定向测试、全量测试、installer e2e 和 `git diff --check` 全绿。
- [ ] 完成 Phase 3 spec 判断、提交计划和用户确认后再提交；本任务结束前不代替后续集成审计。
