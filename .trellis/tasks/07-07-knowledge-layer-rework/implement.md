# Implement：知识库重构——执行计划

## 总体策略（作者已拍板，2026-07-07）

**管线 + 内容一次做完，单任务整体交付。** 先 P1 接通管线（样例条目验证），随后 P2 全量内容重写；内容仍按批次推进——批次是内部工序（保证审查质量与进度可见），不是交付边界。全部批次过审后统一入 RUNTIME_ENTRIES、下线旧 references、收尾交付。

批序：流派（对写作影响最大、条数少）→ 题材 → 节拍 → 场景（含从模板抽取）→ 追读。

## P1 管线

- [x] P1.1 数据层脚手架：`知识/` 目录结构 + 条目 front matter 格式约定 + 每目录 2-3 条**样例条目**（从现 references 手工改写，兼作 P2 重写范本，同样过内容审查）
- [x] P1.2 `路由.csv` 定稿（列瘦身）+ 建书别名归一函数（纯字符串匹配，可测）
- [x] P1.3 book.yaml 增 `题材`/`流派` 字段（读写 + 校验；流派可空）
- [x] P1.4 序1 DTO：附命中条目全文 + 期望产物更新（蒸馏四节结构，含恩怨清算档位：有仇必报/恩怨分明/留有余地）；persist-book 落契约文件 + book.yaml 题材/流派字段
- [x] P1.5 序6 DTO：节拍索引 + 钩子清单 + 场景索引 + 场景候选扫描（关键词匹配卷纲段落与上一章结尾）
- [x] P1.6 细纲三字段：解析 + persist-outline 校验（可空、可自定义文本、场景多值）
- [x] P1.7 备料注入：契约常驻锚点 + 按声明查表注入「落笔时」节 + 三处降级路径
- [x] P1.8 review-input 毒点注入 + `roles/编辑审.md` 增毒点核对节
- [x] P1.9 installer：`知识/` 进 RUNTIME_ENTRIES + 哈希清单 + update 流验证
- [x] P1.10 SKILL.md 措辞同步（序1/序6/写章流程；只写指令）
- [x] P1.11 测试：A1-A5 行为用例（含降级路径 A3、未命中建书 A1）
- [x] P1.12 spec 回填（§5/§6.1/§7/§8 + 版本推进 + 决策记录）

验证命令：`npm test`（v7 目录）；手工冒烟：临时工作目录走一次建书→细纲→备料→review-input，肉眼核对注入切片。

## P2 内容重写（批次为内部工序，每批同构三步）

每批流程：改写 → 独立上下文内容审查（五项清单，记录落 `research/content-review/`）→ 作者抽查签收。全部批次签收后统一生效（门3）。

- [x] P2.1 批1 流派：从 GR 表 + 模板拆出流派条目（退婚流/系统流/赘婿流/黑暗流/学院流/无限流/替身文…），三节结构重写
- [x] P2.2 批2 题材：~15 canonical 题材文件（GR 归位 + 模板题材部分 + taxonomy 题材适配表并入 + front matter `恩怨清算默认` 档位）
- [x] P2.x 横切审计（每批内）：反和解纪律覆盖核查——毒点是否涵盖软收尾/说教式和解/圣母/洗白类禁忌，缺的补进对应条目，不建中央规则文件；同时核查毒点措辞不误伤角色设计自由（约束叙事结果与主角，不禁圣母/和事佬配角人设）
- [x] P2.3 批3 节拍：104 条 PA 每条一文件（检索时代列瘦身，三节重排）
- [x] P2.4 批4 场景：模板桥段详解抽出重写 + 首批新写场景（拍卖会/突破/打斗/谈判等高频）
- [x] P2.5 批5 追读：taxonomy 拆条（钩子类型文件）
- [x] P2.6 references/ 旧结构下线（迁移报告更新；确认无引用后删除）

## 评审门

- 门1（P1 完成）：行为测试绿 + 手工冒烟单 + spec diff 给作者过目。
- 门2（每个 P2 批次）：内容审查记录 + 抽查清单交作者签收。
- 门3（收尾）：全量 A5 格式校验 + installer e2e + 旧 references 下线确认。

## 回滚点

- P1 任一步：installer 未含 `知识/` 前，运行时零影响；含入后回滚 = RUNTIME_ENTRIES 去项。
- P2 批次独立：未签收批次不入生效范围，回滚即弃该批工作副本。
- references/ 在 P2.6 前保持原样，全程可退回现状。

## 拍板记录（2026-07-07，作者）

1. 执行策略：管线 + 内容一次做完，单任务整体交付（批次为内部工序）。
2. 形式维度：并入流派，book.yaml 不设第三字段；直播文按网文分类实践归 `流派/直播流`（挂都市等大类下的写法流派，可跨背景）；知乎短篇删除不迁——v7 只考虑长篇，短篇/平台差异后置。
3. 存量/迁移书补蒸馏：后置，先看 P1 形态；本任务不含。
