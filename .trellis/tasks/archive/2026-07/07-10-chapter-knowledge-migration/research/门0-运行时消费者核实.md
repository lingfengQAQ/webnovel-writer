# 门 0：章级运行时消费者核实（2026-07-20）

## 0.2 切片标题真源

`src/knowledge/index.js:230` 读取切片时双读：`extractSection(body, '规划这一章时') || extractSection(body, '规划时')`。

**定夺**：正式条目统一用 `## 规划时`；批次 1 删除 `'规划这一章时'` 兼容分支（一处真源，无生产旧书无需双读）。`落笔时/审稿时` 标题与读取器一致，消费者：

- `规划时`：序6 候选展示（`toPlanningCandidate`）。
- `落笔时`：`src/prep/index.js:241` 精准注入。
- `审稿时`：`src/review/index.js:35` 核对注入。

## 0.3 条件式字段定夺

| 字段 | 消费证据 | 定夺 |
|---|---|---|
| 关键词（节拍/场景/追读） | `queryScore`（index.js:407）把全部索引字段含关键词展开为 terms 与章级语料匹配打分；chapterKnowledge（dto.js:115）用「本章任务+卷纲+上章末 300 字」作语料调用 | **三维均保留**。design 3.1 原判「仅场景条件保留」作废——运行时框架已把候选扫描统一为四维语料打分，关键词是排序真实输入。策展时关键词须为低误触词（避免「的/人」类高频词），审查项加入误触检查 |
| 适用题材（四维） | `src/` 零读取（grep 无命中） | **删除**：字段、索引无此字段（本就未注册）、format 测试若有断言一并删；11+ 条快穿/种田口径随之消失 |
| 毒点（章级 FM） | `src/` 零读取；审稿只读正文 `审稿时`（review/index.js:35） | **删除 FM 毒点**；判据单一真源在 `## 审稿时`。format 测试现强制 FM 毒点（review 文档 Major 1），批次 1 反转为禁止 |
| 技法 触发问题 | `queryScore` terms + 索引字段已注册 | 保留，策展时按真实章节问题写 |

## 0.4 来源盘点

| 来源 | 计数 | 备注 |
|---|---|---|
| references/节拍 | 102 | 与 README 基线一致 |
| references/场景 | 16 | 同上 |
| references/追读 | 10 | 同上 |
| webnovel-writer/references/csv/写作技法.csv | 待批次 4 实测数据行 | 模板记 104 |
| webnovel-writer/references/csv/场景写法.csv | 待批次 3 实测 | 模板记 96 |
| webnovel-writer/references/csv/桥段套路.csv | 待批次 6 实测 | 模板记 108 |
| 故事对象任务转交篇章 | 待门 0 延伸提取（归档 research 裁决 CSV 中「转交」行） | review 记 11 条 |
| 快穿/种田节拍条目 | 待批次 2 扫描实测 | PRD 记 11、review 记约 12 |

## 结论

design 3.1 修订：节拍 FM = `编号/名称/一句话/关键词`；场景、追读 FM = `名称/一句话/关键词`；技法 FM = `名称/类别/作用层级/触发问题/一句话`。四维正文三切片 `规划时/落笔时/审稿时`，FM 不留毒点、适用题材。运行时窄修正仅一处：删 `规划这一章时` 双读分支。
